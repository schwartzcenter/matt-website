// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://matt.dev', // Update with your actual domain
  integrations: [],
});
